<% provide(:title, "#{t(:letrisms)}") %>

<section class="my-8 flow-root">
  <div class="containerGrid">
    <ul>
      <!-- ULTIMO DE LA GALLERIA DE ENTRADA -->
      <li class="description">
        <h1 class="intro-item">
          <img src="/static/equills.svg" alt="eQuills Logo">
        </h1>
        <p class="intro-item">eQuills es el término que adoptamos para referirnos a las herramientas de escritura digital que desarrollamos con el objetivo de dejar marcas imposibles de crear en el mundo físico. Suena bien ¿Cierto? Pruébalo tú mismo.</p>
        <% if @equill.present? %>
        <a href="/notebook/<%= @equill.slug %>" title="Comenzar" class="change-page intro-item"><button>COMENZAR</button></a>
        <% end %>
      </li>
    </ul>
  </div>
</section>

<script>
  var letrisms = [
      <% @letrisms.each do |letrism| %>
        {
          src: `<%= letrism.img.medium.url %>`,
          href: `<%= letrism_path(letrism.slug) %>`,
          author: `<%= letrism.user.username %>`,
        },
      <% end %>
    ]
  var letrismsTotal = <%= @letrisms.count %>
</script>